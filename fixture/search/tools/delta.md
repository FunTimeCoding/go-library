# Embedding Model

Vector embeddings capture semantic meaning beyond exact keyword
matches. The embedding model converts text chunks into dense
vectors stored alongside the document.

Cosine distance measures similarity between query and document
vectors. Documents closer in vector space rank higher in
similarity search results.
