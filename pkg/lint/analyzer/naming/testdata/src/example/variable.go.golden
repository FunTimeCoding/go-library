package example

var (
	l                  = "test"   // want `use "l" or "locator" instead of "url" in url`
	locator            = "test"   // ok: uses the suggestion
	directoryName      = "test"   // want `use "directory" instead of "dir" in dirName`
	directory          = "test"   // ok: contains the suggestion
	DirSomething       = "test"   // want `use "directory" instead of "dir" in DirSomething`
	OutputDirectory    = ""       // ok: segment is "directory"
	x                  = ""       // want `use "x" instead of "ctx" in ctx`
	messageText        = ""       // want `use "message" instead of "msg" in msgText`
	paramCount         = 0        // want `use "parameter" instead of "param" in paramCount`
	parameterCount     = 0        // ok: contains the suggestion
	modelContextServer = ""       // want `use "model_context" instead of "mcp" in mcpServer`
	clean              = ""       // ok: no blacklisted segment
	r                  = ""       // want `use "r" or "request" instead of "req" in req`
	request            = ""       // ok: uses the suggestion
	documentPath       = ""       // want `use "document" instead of "doc" in docPath`
	document           = ""       // ok: uses the suggestion
	c                  = ""       // want `use "c" or "configuration" instead of "cfg" in cfg`
	m                  = ""       // want `use "m" or "model" instead of "llm" in llm`
	t                  = ""       // want `use "t" instead of "tmp" in tmp`
	loginHandler       = ""       // want `avoid "handler" in name, use a more specific term`
	userInfo           = ""       // want `avoid "info" in name, use a more specific term`
	userData           = ""       // want `avoid "data" in name, use a more specific term`
	positionX          = 0        // want `use "position" instead of "pos" in posX`
	position           = 0        // ok: uses the suggestion
	bufferData         = []byte{} // want `use "buffer" instead of "buf" in bufData`
	buffer             = []byte{} // ok: uses the suggestion
	pointerValue       = 0        // want `use "pointer" instead of "ptr" in ptrValue`
	addressStr         = ""       // want `use "address" instead of "addr" in addrStr`
	referenceCount     = 0        // want `use "reference" instead of "ref" in refCount`
	navigationBar      = ""       // want `use "navigation" instead of "nav" in navBar`
	pastState          = ""       // want `use "past" instead of "prev" in prevState`
	past               = ""       // ok: uses the suggestion
	markupData         = []byte{} // want `use "markup" instead of "yaml" in yamlData`
	markupContent      = []byte{} // want `use "markup" instead of "xml" in xmlContent`
	markupPage         = ""       // want `use "markup" instead of "html" in htmlPage`
	notationPayload    = []byte{} // want `use "notation" instead of "json" in jsonPayload`
	declarationCount   = 0        // want `use "declaration" instead of "decl" in declCount`
)
