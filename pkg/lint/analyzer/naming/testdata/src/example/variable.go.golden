package example

var l = "test"                    // want `use "l" or "locator" instead of "url" in url`
var locator = "test"              // ok: uses the suggestion
var directoryName = "test"        // want `use "d" or "directory" instead of "dir" in dirName`
var directory = "test"            // ok: contains the suggestion
var DirSomething = "test"         // want `use "d" or "directory" instead of "dir" in DirSomething`
var OutputDirectory = ""          // ok: segment is "directory"
var x = ""                        // want `use "x" instead of "ctx" in ctx`
var messageText = ""              // want `use "m" or "message" instead of "msg" in msgText`
var paramCount = 0                // want `use "parameter" instead of "param" in paramCount`
var parameterCount = 0            // ok: contains the suggestion
var modelContextServer = ""       // want `use "c" or "model_context" instead of "mcp" in mcpServer`
var clean = ""                    // ok: no blacklisted segment
var r = ""                        // want `use "r" or "request" instead of "req" in req`
var request = ""                  // ok: uses the suggestion
var documentPath = ""             // want `use "d" or "document" instead of "doc" in docPath`
var document = ""                 // ok: uses the suggestion
var c = ""                        // want `use "c" or "configuration" instead of "cfg" in cfg`
var m = ""                        // want `use "m" or "model" instead of "llm" in llm`
var t = ""                        // want `use "t" instead of "tmp" in tmp`
var loginHandler = ""             // want `avoid "handler" in name, use a more specific term`
var userInfo = ""                 // want `avoid "info" in name, use a more specific term`
var userData = ""                 // want `avoid "data" in name, use a more specific term`
var positionX = 0                 // want `use "p" or "position" instead of "pos" in posX`
var position = 0                  // ok: uses the suggestion
var bufferData = []byte{}         // want `use "b" or "buffer" instead of "buf" in bufData`
var buffer = []byte{}             // ok: uses the suggestion
var pointerValue = 0              // want `use "p" or "pointer" instead of "ptr" in ptrValue`
var addressStr = ""               // want `use "a" or "address" instead of "addr" in addrStr`
var referenceCount = 0            // want `use "r" or "reference" instead of "ref" in refCount`
var navigationBar = ""            // want `use "n" or "navigation" instead of "nav" in navBar`
var pastState = ""                // want `use "past" instead of "prev" in prevState`
var past = ""                     // ok: uses the suggestion
var markupData = []byte{}         // want `use "m" or "markup" instead of "yaml" in yamlData`
var markupContent = []byte{}      // want `use "m" or "markup" instead of "xml" in xmlContent`
var markupPage = ""               // want `use "m" or "markup" instead of "html" in htmlPage`
var notationPayload = []byte{}    // want `use "j" or "notation" instead of "json" in jsonPayload`
var declarationCount = 0          // want `use "declaration" instead of "decl" in declCount`
