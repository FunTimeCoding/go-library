package constant

const (
	Name = "goterraformd"

	Trigger          = "trigger"
	Runs             = "runs"
	Run              = "run"
	Limit            = "limit"
	Status           = "status"
	Identifier       = "id"
	ServerInstructions = "Terraform apply runner — trigger init + apply and check results. Runs execute asynchronously; use runs and run tools to poll for completion."

	RecentRunsFailed = "failed to list recent runs"
	RunLookupFailed  = "failed to look up run"
)
