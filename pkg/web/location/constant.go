package location

const (
	Root = "/"

	Callback = "/callback"
	Favicon  = "/favicon.ico"
	Login    = "/login"
	Logout   = "/logout"
	Metrics  = "/metrics"
	Shutdown = "/shutdown"
	Status   = "/status"

	Echo    = "/echo"
	Monitor = "/monitor"
)
